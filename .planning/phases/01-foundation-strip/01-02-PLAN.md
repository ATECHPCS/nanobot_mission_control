---
phase: 01-foundation-strip
plan: 02
type: execute
wave: 2
depends_on: ["01-01", "01-01b"]
files_modified:
  - src/index.ts
  - src/types/shared.ts
  - src/lib/db.ts
  - src/store/index.ts
  - src/proxy.ts
  - src/lib/auth.ts
autonomous: true
requirements: [FOUN-01]

must_haves:
  truths:
    - "No duplicate Zustand store exists at src/index.ts"
    - "Entity types are defined once in src/types/shared.ts and imported by both server and client"
    - "safeCompare is defined once in auth.ts and imported by proxy.ts"
    - "No any types remain in files touched by this phase"
  artifacts:
    - path: "src/types/shared.ts"
      provides: "Canonical entity type definitions (Task, Agent, Comment, etc.)"
      exports: ["Task", "Agent", "Comment", "Notification", "Activity"]
      min_lines: 30
    - path: "src/lib/auth.ts"
      provides: "safeCompare export"
      contains: "export.*safeCompare"
  key_links:
    - from: "src/lib/db.ts"
      to: "src/types/shared.ts"
      via: "import { Task, Agent, ... } from '@/types/shared'"
      pattern: "import.*from.*types/shared"
    - from: "src/store/index.ts"
      to: "src/types/shared.ts"
      via: "import { Task, Agent, ... } from '@/types/shared'"
      pattern: "import.*from.*types/shared"
    - from: "src/proxy.ts"
      to: "src/lib/auth.ts"
      via: "import { safeCompare } from '@/lib/auth'"
      pattern: "import.*safeCompare.*from.*auth"
---

<objective>
Clean up known tech debt: delete the duplicate Zustand store, consolidate type definitions into shared.ts, fix the duplicated safeCompare function, and replace any types in touched files.

Purpose: Eliminate tech debt identified in the codebase map so the foundation is clean for future phases. These are isolated fixes that don't affect OC stripping but improve code quality.
Output: Clean type system, single store, no duplicate utility functions, proper error typing.
</objective>

<execution_context>
@/Users/designmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/designmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-strip/01-CONTEXT.md
@.planning/phases/01-foundation-strip/01-RESEARCH.md
@.planning/phases/01-foundation-strip/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete duplicate store and consolidate types into shared.ts</name>
  <files>
    src/index.ts
    src/types/shared.ts
    src/lib/db.ts
    src/store/index.ts
  </files>
  <action>
1. Delete src/index.ts (760 lines, dead duplicate Zustand store). Grep confirmed no imports reference it. Verify with: `grep -r "from.*['\"]@/index['\"]\\|from.*['\"]\\.\\.*/index['\"]\\|from.*['\"]\\./index['\"]" src/ --include="*.ts" --include="*.tsx"` -- should return zero results. If any imports exist, update them to use src/store/index.ts instead.

2. Create or expand src/types/shared.ts with canonical entity type definitions. Extract types from both src/lib/db.ts (server-side definitions) and src/store/index.ts (client-side definitions). Reconcile drift:
   - priority field: server allows 'urgent' but not 'critical', client allows both. Canonical definition should include BOTH: 'low' | 'medium' | 'high' | 'critical' | 'urgent'
   - Include at minimum: Task, Agent, Comment, Notification, Activity, CronJob, WebhookEndpoint, User/Session types

3. Update src/lib/db.ts to import types from src/types/shared.ts instead of defining them inline. Remove duplicate type definitions.

4. Update src/store/index.ts to import types from src/types/shared.ts instead of defining them inline. Remove duplicate type definitions.

5. Run `pnpm build` to verify all type imports resolve correctly and no type errors introduced.
  </action>
  <verify>
    <automated>cd /Users/designmac/projects/nanobot_mission_control && test ! -f src/index.ts && echo "DUPLICATE_STORE_DELETED" && grep -l "from.*types/shared" src/lib/db.ts src/store/index.ts && pnpm build 2>&1 | tail -3</automated>
  </verify>
  <done>src/index.ts deleted. src/types/shared.ts contains canonical type definitions. Both db.ts and store/index.ts import from shared.ts. Priority field drift resolved. pnpm build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Fix safeCompare duplication and replace any types in touched files</name>
  <files>
    src/lib/auth.ts
    src/proxy.ts
    src/lib/config.ts
    src/lib/super-admin.ts
  </files>
  <action>
1. Fix safeCompare duplication:
   - Ensure safeCompare is exported from src/lib/auth.ts (add `export` keyword if not already exported)
   - In src/proxy.ts: delete the local safeCompare implementation (which has a timing leak -- returns false immediately on length mismatch). Replace with: `import { safeCompare } from '@/lib/auth';` (or appropriate relative path)
   - Verify the auth.ts version uses constant-time comparison for ALL cases including length mismatch

2. Replace `any` types in files touched by this phase. Focus ONLY on files already modified in Plan 01 or this plan -- do NOT touch untouched files. Use this pattern for catch blocks:
   ```typescript
   catch (err: unknown) {
     const message = err instanceof Error ? err.message : String(err);
     logger.error('Failed:', message);
   }
   ```

   Target files (only if they have `any` types):
   - src/lib/config.ts
   - src/lib/super-admin.ts
   - src/store/index.ts
   - src/app/api/agents/route.ts
   - src/app/api/cron/route.ts
   - src/app/api/chat/messages/route.ts
   - src/app/api/integrations/route.ts
   - src/proxy.ts

   For each: search for `: any` and replace with proper types. Use `unknown` for catch blocks, specific types for function parameters and return values where inferable.

3. Run `pnpm build` to verify all changes compile correctly.
  </action>
  <verify>
    <automated>cd /Users/designmac/projects/nanobot_mission_control && grep -n "safeCompare" src/proxy.ts | head -3 && pnpm build 2>&1 | tail -3</automated>
  </verify>
  <done>safeCompare is exported from auth.ts and imported in proxy.ts (no local duplicate). any types replaced with proper types in all files touched by this phase. pnpm build succeeds.</done>
</task>

</tasks>

<verification>
1. `test ! -f src/index.ts` -- duplicate store deleted
2. `grep -c "from.*types/shared" src/lib/db.ts src/store/index.ts` -- both import from shared.ts
3. `grep -c "safeCompare" src/proxy.ts` -- shows import, not local definition
4. `pnpm build` succeeds
</verification>

<success_criteria>
- src/index.ts (duplicate store) no longer exists
- src/types/shared.ts exists with canonical types imported by db.ts and store/index.ts
- safeCompare defined once in auth.ts, imported in proxy.ts
- No `any` types in files touched by Phase 1 plans
- pnpm build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-strip/01-02-SUMMARY.md`
</output>

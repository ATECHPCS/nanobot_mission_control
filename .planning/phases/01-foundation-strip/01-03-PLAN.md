---
phase: 01-foundation-strip
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - tests/fixtures/
  - src/lib/__tests__/webhooks-smoke.test.ts
  - src/lib/__tests__/sse-smoke.test.ts
autonomous: true
requirements: [FOUN-03, FOUN-04, FOUN-05, FOUN-06, FOUN-07, FOUN-08]

must_haves:
  truths:
    - "All existing E2E tests pass (updated for nanobot context)"
    - "Webhook delivery works without OpenClaw event sources"
    - "SSE activity feed connects and delivers events"
    - "Auth login/logout flow works"
    - "Kanban CRUD operations work"
    - "RBAC role checks function correctly"
  artifacts:
    - path: "src/lib/__tests__/webhooks-smoke.test.ts"
      provides: "Smoke test verifying webhook delivery without OC events"
      min_lines: 15
    - path: "src/lib/__tests__/sse-smoke.test.ts"
      provides: "Smoke test verifying SSE connection without OC events"
      min_lines: 15
  key_links:
    - from: "src/lib/__tests__/webhooks-smoke.test.ts"
      to: "src/lib/webhooks.ts"
      via: "import and invoke webhook delivery"
      pattern: "import.*webhooks"
    - from: "src/lib/__tests__/sse-smoke.test.ts"
      to: "src/lib/event-bus.ts"
      via: "import and invoke event broadcasting"
      pattern: "import.*event-bus"
---

<objective>
Update E2E tests for nanobot context, add smoke tests for webhook and SSE systems, and run the full test suite to verify all existing features work after the strip.

Purpose: Prove that the codebase surgery in Plans 01-02 did not break any existing functionality. This is the verification gate for Phase 1.
Output: Green test suite confirming auth, RBAC, kanban, webhooks, and SSE all work standalone.
</objective>

<execution_context>
@/Users/designmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/designmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-strip/01-CONTEXT.md
@.planning/phases/01-foundation-strip/01-RESEARCH.md
@.planning/phases/01-foundation-strip/01-01-SUMMARY.md
@.planning/phases/01-foundation-strip/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update E2E tests and fixtures for nanobot context</name>
  <files>
    tests/fixtures/
    tests/helpers.ts
  </files>
  <action>
1. Delete OC-specific E2E spec files if they still exist:
   - tests/gateway-connect.spec.ts (or similar name)
   - tests/device-identity.spec.ts (or similar name)
   - tests/direct-cli.spec.ts (or similar name)
   Search: `ls tests/*.spec.ts` and identify any that test OC-specific functionality.

2. Delete tests/fixtures/openclaw/ directory if it exists and wasn't already deleted in Plan 01.

3. Review tests/helpers.ts: Remove any OC fixture path setup, OC environment variable configuration, or OC-specific test setup code. Update to use NANOBOT_* env vars if test helpers reference environment variables.

4. Run the remaining E2E specs individually to identify failures:
   `pnpm test:e2e --reporter=list 2>&1 | head -100`

   For each failure:
   - If failure is due to OC reference in test code: update the test to remove OC dependency
   - If failure is due to OC reference in app code: something was missed in Plan 01 -- fix the app code
   - If failure is due to deleted fixture: update test to use remaining fixtures or create minimal nanobot-context fixture

5. Verify existing auth/RBAC tests pass:
   `npx playwright test tests/login-flow.spec.ts tests/auth-guards.spec.ts` (or equivalent file names)

6. Verify kanban tests pass:
   `npx playwright test tests/tasks-crud.spec.ts` (or equivalent file name)

Run full suite: `pnpm test:e2e` -- all specs must pass.
  </action>
  <verify>
    <automated>cd /Users/designmac/projects/nanobot_mission_control && pnpm test:e2e 2>&1 | tail -10</automated>
  </verify>
  <done>OC-specific test files deleted. Test fixtures updated. All remaining E2E tests pass with zero OC dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Add smoke tests for webhooks and SSE</name>
  <files>
    src/lib/__tests__/webhooks-smoke.test.ts
    src/lib/__tests__/sse-smoke.test.ts
  </files>
  <action>
Create smoke tests to verify webhook and SSE subsystems work without OC event sources. These are unit-level tests using Vitest (not E2E).

1. Create src/lib/__tests__/webhooks-smoke.test.ts:
   - Import the webhook delivery function from src/lib/webhooks.ts
   - Test that a webhook endpoint can be registered (or mock the DB call)
   - Test that an event dispatched via eventBus triggers webhook delivery
   - Test that delivery retries on failure (if retry logic exists)
   - Verify NO references to OpenClaw event types
   - Use Vitest's vi.mock for database and HTTP calls

2. Create src/lib/__tests__/sse-smoke.test.ts:
   - Import the event bus from src/lib/event-bus.ts
   - Test that eventBus.broadcast() emits events
   - Test that a subscriber receives broadcast events
   - Test that the SSE endpoint format is correct (event: type, data: json)
   - Verify NO references to OpenClaw event types
   - Use Vitest for assertions

Examine the actual webhook and event-bus source files first to understand their API surface before writing tests. Match the existing test patterns in the codebase (check src/lib/__tests__/ or tests/ for style conventions).

Run: `pnpm test -- --grep "smoke"` or `pnpm test src/lib/__tests__/webhooks-smoke.test.ts src/lib/__tests__/sse-smoke.test.ts`

Then run full verification:
- `pnpm build` (still passes)
- `pnpm test` (all unit tests pass including new smoke tests)
- `grep -r "openclaw\|OpenClaw\|OPENCLAW" src/ --include="*.ts" --include="*.tsx" -l` (zero results -- final OC reference check)
  </action>
  <verify>
    <automated>cd /Users/designmac/projects/nanobot_mission_control && pnpm test 2>&1 | tail -10 && echo "---" && pnpm build 2>&1 | tail -3 && echo "---" && grep -r "openclaw\|OpenClaw\|OPENCLAW" src/ --include="*.ts" --include="*.tsx" -c 2>/dev/null; echo "OC_REFS_EXIT:$?"</automated>
  </verify>
  <done>Webhook smoke test and SSE smoke test both pass. Full unit test suite green. Full build green. Zero OpenClaw references confirmed. Phase 1 complete.</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds
2. `pnpm test` -- all unit tests pass (including new smoke tests)
3. `pnpm test:e2e` -- all E2E tests pass
4. `grep -r "openclaw\|OpenClaw\|OPENCLAW\|OC_" src/ --include="*.ts" --include="*.tsx"` returns zero results
5. `grep -r "OPENCLAW_\|OC_" .env* docker-compose.yml Dockerfile` returns zero results
6. Smoke tests exist for webhooks and SSE
</verification>

<success_criteria>
- All E2E tests pass (OC-specific ones deleted, remaining ones updated)
- Webhook smoke test passes (delivery works without OC events)
- SSE smoke test passes (event bus works without OC events)
- Full build + test suite green
- Zero OpenClaw references anywhere in the project (final gate)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-strip/01-03-SUMMARY.md`
</output>

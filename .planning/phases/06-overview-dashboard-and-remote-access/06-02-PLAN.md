---
phase: 06-overview-dashboard-and-remote-access
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/dashboard/overview-landing.tsx
  - src/app/[[...panel]]/page.tsx
  - src/components/dashboard/dashboard.tsx
  - src/components/layout/live-feed.tsx
  - src/components/agents/agents-panel.tsx
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]

must_haves:
  truths:
    - "Landing page shows all agents as composite status cards with name, status, last activity, channel health, and token/message usage"
    - "Activity feed on landing page shows real-time agent events via SSE without manual refresh"
    - "Error counts are displayed prominently and grouped by agent with filtering"
    - "Summary metric strip shows fleet-level health at a glance"
    - "Navigating to /overview or / shows the new agent-centric landing page"
    - "Navigating to /agents redirects to the landing page (not a dead route)"
    - "Agent card slide-out still works from the landing page"
  artifacts:
    - path: "src/components/dashboard/overview-landing.tsx"
      provides: "New landing page composing MetricStrip, AgentCardGrid, ActivityFeedInline, ErrorSummaryPanel"
      min_lines: 60
  key_links:
    - from: "src/components/dashboard/overview-landing.tsx"
      to: "src/components/agents/agent-card-grid.tsx"
      via: "import and render AgentCardGrid"
      pattern: "AgentCardGrid"
    - from: "src/components/dashboard/overview-landing.tsx"
      to: "src/components/dashboard/metric-strip.tsx"
      via: "import and render MetricStrip"
      pattern: "MetricStrip"
    - from: "src/components/dashboard/overview-landing.tsx"
      to: "src/components/dashboard/activity-feed-inline.tsx"
      via: "import and render ActivityFeedInline"
      pattern: "ActivityFeedInline"
    - from: "src/components/dashboard/overview-landing.tsx"
      to: "src/components/dashboard/error-summary-panel.tsx"
      via: "import and render ErrorSummaryPanel"
      pattern: "ErrorSummaryPanel"
    - from: "src/app/[[...panel]]/page.tsx"
      to: "src/components/dashboard/overview-landing.tsx"
      via: "ContentRouter 'overview' case"
      pattern: "case.*overview.*OverviewLanding"
---

<objective>
Create the OverviewLanding component that composes all new and existing components into the unified agent-centric landing page, wire it into the ContentRouter, remove the live-feed sidebar from the page layout, delete legacy files, and verify the complete landing page works.

Purpose: This is the final assembly -- connecting the components from Plan 01 with existing agent components into the landing page the user described: metric strip at top, agent card grid in the middle, activity feed + error summary in a two-column row below.
Output: Working landing page at /overview that replaces the legacy dashboard.
</objective>

<execution_context>
@/Users/designmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/designmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-overview-dashboard-and-remote-access/06-CONTEXT.md
@.planning/phases/06-overview-dashboard-and-remote-access/06-RESEARCH.md
@.planning/phases/06-overview-dashboard-and-remote-access/06-01-SUMMARY.md

@src/app/[[...panel]]/page.tsx
@src/components/agents/agents-panel.tsx
@src/components/agents/agent-card-grid.tsx
@src/components/agents/agent-slide-out.tsx
@src/lib/use-smart-poll.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OverviewLanding and wire into ContentRouter</name>
  <files>src/components/dashboard/overview-landing.tsx, src/app/[[...panel]]/page.tsx</files>
  <action>
**overview-landing.tsx:**
- Client component ("use client")
- Import and compose: MetricStrip (from Plan 01), AgentCardGrid, AgentSlideOut, ActivityFeedInline (from Plan 01), ErrorSummaryPanel (from Plan 01)
- Extract the fetch + polling logic from agents-panel.tsx: useSmartPoll + fetchAgents call to /api/agents/discover. Do NOT duplicate -- move the fetch responsibility here since this is now the host component for agent data.
- Manage selectedDiscoveredAgentId state for the slide-out (same pattern as agents-panel.tsx)
- Layout (stacked, per user decision):
  1. MetricStrip (full width, top)
  2. AgentCardGrid (full width, middle) -- with slide-out support
  3. Two-column row (below grid): ActivityFeedInline (left, wider ~60%) | ErrorSummaryPanel (right ~40%)
- Use Tailwind: flex flex-col gap-6 for vertical stack; grid grid-cols-1 lg:grid-cols-5 gap-6 for the bottom row (3 cols activity, 2 cols errors)
- Quick action buttons row between metric strip and agent grid: Tasks, Sessions, Tokens, Settings -- each navigates to the corresponding panel via router
- Pass agents data to MetricStrip as prop
- AgentSlideOut renders conditionally when selectedDiscoveredAgentId is set

**page.tsx:**
- Import OverviewLanding (lazy or direct)
- In ContentRouter switch:
  - Change 'overview' case: render `<OverviewLanding />` instead of `<Dashboard />`
  - Add 'agents' case: render `<OverviewLanding />` (backward compat for bookmarks/redirects)
  - Remove 'spawn' case if it exists (or let it fall through to default)
  - Remove 'webhooks' case if it exists (or let it fall through to default)
- Remove the LiveFeed sidebar rendering: delete the `{liveFeedOpen && <LiveFeed ... />}` block
- Remove the live-feed toggle button from the page layout
- Remove liveFeedOpen/toggleLiveFeed from the store destructure in this file
- Remove LiveFeed import
- The flex layout should naturally expand (content div already has flex-1)

**Token display on agent cards (DASH-02):**
- Check if AgentCardGrid/agent cards already show token data. If not, the card component needs a small addition: show message count from the agent's session data. Per decision [04-03], nanobot agents have message counts not token counts. Display as "N messages" on each card. If gateway API returns token data in the health snapshot, show "NK tokens" instead. This may require a minor update to the agent card component -- if so, add it to files_modified.
  </action>
  <verify>
    <automated>cd /Users/designmac/projects/nanobot_mission_control && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>OverviewLanding renders the full stacked layout. ContentRouter routes /overview and /agents to OverviewLanding. LiveFeed sidebar removed from page layout. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Delete legacy files and clean up dead imports</name>
  <files>src/components/dashboard/dashboard.tsx, src/components/layout/live-feed.tsx, src/components/agents/agents-panel.tsx</files>
  <action>
- Delete src/components/dashboard/dashboard.tsx (770-line legacy overview -- fully replaced by overview-landing.tsx)
- Delete src/components/layout/live-feed.tsx (sidebar removed -- activity is inline now)
- Evaluate src/components/agents/agents-panel.tsx: if its fetch/poll logic has been moved to overview-landing.tsx and no other file imports it, delete it. If other files import it, keep it as a thin re-export or redirect.
- Grep the codebase for any remaining imports of Dashboard, LiveFeed, or AgentsPanel: `grep -rn 'from.*dashboard/dashboard\|from.*layout/live-feed\|from.*agents/agents-panel' src/`
- Fix any broken imports found by the grep (likely just page.tsx which was already updated in Task 1)
- Run TypeScript compilation to confirm no dangling references
  </action>
  <verify>
    <automated>cd /Users/designmac/projects/nanobot_mission_control && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Legacy dashboard.tsx and live-feed.tsx deleted. No dangling imports. TypeScript compiles cleanly. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete agent-centric landing page replacing the legacy overview dashboard. The landing page shows: (1) metric strip with fleet health summary, (2) agent card grid with slide-out detail panel, (3) inline activity feed with real-time SSE events, (4) error summary panel grouped by agent. Nav rail cleaned up (no Agents/Webhooks/Spawn items). Live-feed sidebar removed entirely.</what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Navigate to http://localhost:3000 (should load the new landing page)
    3. Verify the metric strip shows agent counts, status breakdown, error count, active sessions
    4. Verify agent cards are displayed (same cards as before, now on the landing page)
    5. Click an agent card -- slide-out panel should open with full details
    6. Check the activity feed (left column below cards) shows real-time events
    7. Check the error summary (right column below cards) groups errors by agent with collapse/expand
    8. Verify nav rail: no "Agents", "Webhooks", or "Spawn" items visible
    9. Verify no live-feed sidebar toggle in the header
    10. Navigate to /agents URL -- should show the same landing page (backward compat)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- Landing page loads at /overview with all four sections visible
- Agent cards are interactive (click opens slide-out)
- Activity feed updates in real-time via SSE
- Error panel groups errors by agent
- No legacy dashboard content visible (no GitHub stats, system health, backups, etc.)
- No live-feed sidebar anywhere in the UI
- /agents URL renders the landing page
</verification>

<success_criteria>
- DASH-01: Landing page shows all agents with composite status cards -- VERIFIED by agent card grid on landing page
- DASH-02: Each card shows name, status, last activity, channel health, token/message usage -- VERIFIED by existing card content + token display
- DASH-03: Activity feed shows real-time agent events via SSE -- VERIFIED by inline ActivityFeedInline with SSE subscription
- DASH-04: Error/failure counts displayed prominently with filtering -- VERIFIED by ErrorSummaryPanel with agent grouping and count badges
</success_criteria>

<output>
After completion, create `.planning/phases/06-overview-dashboard-and-remote-access/06-02-SUMMARY.md`
</output>

---
phase: 01-foundation-strip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/device-identity.ts
  - src/lib/agent-sync.ts
  - src/lib/websocket.ts
  - src/components/panels/gateway-config-panel.tsx
  - src/components/panels/multi-gateway-panel.tsx
  - src/components/panels/agent-comms-panel.tsx
  - src/components/panels/agent-squad-panel.tsx
  - src/components/panels/agent-squad-panel-phase3.tsx
  - scripts/e2e-openclaw/
  - src/app/[[...panel]]/page.tsx
  - src/components/layout/nav-rail.tsx
  - src/store/index.ts
  - src/lib/config.ts
  - src/lib/super-admin.ts
  - src/app/api/integrations/route.ts
  - src/app/api/agents/route.ts
  - src/app/api/cron/route.ts
  - src/app/api/chat/messages/route.ts
  - src/components/panels/agent-detail-tabs.tsx
  - .env.example
  - docker-compose.yml
  - Dockerfile
  - openapi.json
autonomous: true
requirements: [FOUN-01, FOUN-02, FOUN-03, FOUN-04, FOUN-05, FOUN-06, FOUN-07]

must_haves:
  truths:
    - "Zero OpenClaw/OPENCLAW/openclaw/OC_ references remain in src/ directory"
    - "Zero OPENCLAW_* or OC_* environment variable references remain anywhere in the project"
    - "pnpm build completes with zero errors"
    - "Nav rail shows no links to deleted panels"
    - "Environment variables use NANOBOT_* namespace"
  artifacts:
    - path: "src/lib/config.ts"
      provides: "Core config with NANOBOT_* env vars, zero OC references"
      contains: "NANOBOT_HOME"
    - path: "src/store/index.ts"
      provides: "Zustand store with OC state slices removed"
    - path: "src/components/layout/nav-rail.tsx"
      provides: "Navigation with OC panels removed"
    - path: ".env.example"
      provides: "Example env with NANOBOT_* variable names"
      contains: "NANOBOT_"
  key_links:
    - from: "src/app/[[...panel]]/page.tsx"
      to: "remaining panel components"
      via: "ContentRouter switch/import"
      pattern: "import.*panel"
    - from: "src/components/layout/nav-rail.tsx"
      to: "ContentRouter panel IDs"
      via: "navigation config matching panel routes"
    - from: "src/lib/config.ts"
      to: ".env.example"
      via: "process.env.NANOBOT_* reads matching .env.example keys"
---

<objective>
Delete all OpenClaw-only files, strip OpenClaw references from mixed files, rename environment variables to NANOBOT_* namespace, and remove dead panels from navigation and routing.

Purpose: Eliminate all OpenClaw protocol code so the codebase is a clean foundation for nanobot-native features. This is the core surgery of Phase 1.
Output: A codebase with zero OpenClaw references that compiles cleanly.
</objective>

<execution_context>
@/Users/designmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/designmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-strip/01-CONTEXT.md
@.planning/phases/01-foundation-strip/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete OpenClaw-only files and fix cascading imports</name>
  <files>
    src/lib/device-identity.ts
    src/lib/agent-sync.ts
    src/lib/websocket.ts
    src/components/panels/gateway-config-panel.tsx
    src/components/panels/multi-gateway-panel.tsx
    src/components/panels/agent-comms-panel.tsx
    src/components/panels/agent-squad-panel.tsx
    src/components/panels/agent-squad-panel-phase3.tsx
    scripts/e2e-openclaw/
    openapi.json
    src/app/[[...panel]]/page.tsx
    src/components/layout/nav-rail.tsx
    src/store/index.ts
  </files>
  <action>
Delete these OpenClaw-only files entirely:
- src/lib/device-identity.ts (Ed25519 device identity)
- src/lib/agent-sync.ts (OpenClaw gateway agent sync)
- src/lib/websocket.ts (WebSocket client for OC gateway)
- src/components/panels/gateway-config-panel.tsx (OC gateway config panel)
- src/components/panels/multi-gateway-panel.tsx (multi-gateway management)
- src/components/panels/agent-comms-panel.tsx (OC agent communication)
- src/components/panels/agent-squad-panel.tsx (621 lines, OC agent squad)
- src/components/panels/agent-squad-panel-phase3.tsx (926 lines, OC agent squad phase 3)
- scripts/e2e-openclaw/ (entire directory, OC E2E test harness)
- openapi.json (documents OC API endpoints -- will be rebuilt in later phases when nanobot APIs exist)

Delete tests/fixtures/openclaw/ directory if it exists.

Then fix all cascading import errors:

1. src/app/[[...panel]]/page.tsx (ContentRouter): Remove imports and case statements for all deleted panel components. Keep imports for: agent-detail-tabs, task-board, settings, super-admin, dashboard panels.

2. src/components/layout/nav-rail.tsx: Remove nav items for deleted panels (gateway-config, multi-gateway, agent-comms, agent-squad). Keep nav items for: dashboard, tasks, agents (detail), settings, super-admin.

3. src/store/index.ts: Remove OC-specific state slices (WebSocket connection state, gateway status, agent sync state). Remove any imports from deleted modules. Keep core state slices (tasks, auth, notifications, activities, settings).

4. src/lib/scheduler.ts: If it imports agent-sync for scheduled sync, remove that import and any OC sync scheduled tasks.

5. Any test files that import deleted modules: remove or update the imports.

Run `pnpm build` after all deletions to verify zero import errors. Fix any additional cascading errors the compiler finds.

IMPORTANT: Update both ContentRouter AND nav-rail.tsx together -- they must stay in sync (see Pitfall 3 in RESEARCH.md).
  </action>
  <verify>
    <automated>cd /Users/designmac/projects/nanobot_mission_control && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>All OpenClaw-only files deleted. ContentRouter and nav-rail updated. Store cleaned of OC state. pnpm build succeeds with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Strip OpenClaw references from mixed files and rename env vars</name>
  <files>
    src/lib/config.ts
    src/lib/super-admin.ts
    src/app/api/integrations/route.ts
    src/app/api/agents/route.ts
    src/app/api/cron/route.ts
    src/app/api/chat/messages/route.ts
    src/components/panels/agent-detail-tabs.tsx
    .env.example
    docker-compose.yml
    Dockerfile
  </files>
  <action>
Strip OpenClaw references from these high-reference mixed files (in priority order):

1. src/lib/config.ts (36 refs): Remove all OPENCLAW_* env var reads. Replace with NANOBOT_* namespace:
   - OPENCLAW_HOME -> NANOBOT_HOME (default: ~/.nanobot)
   - OPENCLAW_STATE_DIR -> NANOBOT_STATE_DIR
   - DELETE entirely: OPENCLAW_GATEWAY_HOST, OPENCLAW_GATEWAY_PORT, NEXT_PUBLIC_GATEWAY_HOST, NEXT_PUBLIC_GATEWAY_PORT (no gateway equivalent yet)
   - Keep MC_* vars as-is (they are Mission Control config, not OC-specific)
   - Remove any OC-specific config sections (gateway config, device identity config)

2. src/lib/super-admin.ts (39 refs): Remove OC provisioning commands (openclaw CLI calls, OC tenant setup). Keep core tenant management logic and role-based admin functions.

3. src/app/api/integrations/route.ts (20 refs): Remove OC environment variable management endpoints. Keep core integration management if any exists.

4. src/app/api/agents/route.ts (17 refs): Remove OC agent sync logic. Keep agent CRUD operations. Agent data will come from filesystem discovery in Phase 2; for now, keep the DB-backed CRUD as a skeleton.

5. src/app/api/cron/route.ts (15 refs): Remove `openclaw cron trigger` command execution. Keep cron infrastructure if it has non-OC uses.

6. src/app/api/chat/messages/route.ts (14 refs): Remove OC gateway chat routing. Keep message storage/retrieval if applicable.

7. src/components/panels/agent-detail-tabs.tsx (12 refs): Remove OC-specific agent fields (device ID, gateway status, OC config). Keep the tab structure for reuse with nanobot agent details in Phase 2.

Then rename env vars in non-TypeScript files:
- .env.example: Rename OPENCLAW_* to NANOBOT_*, delete gateway vars, add comments explaining each var
- docker-compose.yml: Update any OPENCLAW_* references to NANOBOT_*
- Dockerfile: Update any OPENCLAW_* references to NANOBOT_*
- .env.test: If exists, update similarly

For each file, search for patterns: openclaw, OpenClaw, OPENCLAW, oc_, OC_ and remove or replace every match.

After all changes, run comprehensive grep to verify zero OC references remain:
`grep -r "openclaw\|OpenClaw\|OPENCLAW\|OC_" src/ --include="*.ts" --include="*.tsx" -l`
`grep -r "OPENCLAW_\|OC_" .env* docker-compose.yml Dockerfile`

Both greps must return zero results.

Run `pnpm build` after all changes to verify compilation.
  </action>
  <verify>
    <automated>cd /Users/designmac/projects/nanobot_mission_control && pnpm build && echo "---BUILD OK---" && grep -r "openclaw\|OpenClaw\|OPENCLAW" src/ --include="*.ts" --include="*.tsx" -l; echo "EXIT:$?"</automated>
  </verify>
  <done>All 7 mixed files stripped of OpenClaw references. Env vars renamed to NANOBOT_* in config.ts, .env.example, docker-compose.yml, Dockerfile. Grep confirms zero OC references in src/ and config files. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds with zero errors
2. `grep -r "openclaw\|OpenClaw\|OPENCLAW\|OC_" src/ --include="*.ts" --include="*.tsx"` returns zero results
3. `grep -r "OPENCLAW_\|OC_" .env* docker-compose.yml Dockerfile` returns zero results
4. Deleted files no longer exist on disk
5. Nav rail and ContentRouter reference only kept panels
</verification>

<success_criteria>
- Zero OpenClaw references in entire src/ directory
- Zero OPENCLAW_*/OC_* env var references in config files
- pnpm build completes successfully
- 10 OC-only files deleted, 7 mixed files stripped, env vars renamed
- Nav rail shows only: dashboard, tasks, agents, settings, super-admin (or subset)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-strip/01-01-SUMMARY.md`
</output>

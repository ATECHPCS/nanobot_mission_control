---
phase: 01-foundation-strip
plan: 05
type: execute
wave: 2
depends_on: [01-04]
files_modified: []
autonomous: false
requirements: [FOUN-03, FOUN-04, FOUN-05, FOUN-06, FOUN-07, FOUN-08]
gap_closure: true

must_haves:
  truths:
    - "pnpm build completes with exit code 0 (zero TypeScript compilation errors)"
    - "pnpm test passes all unit and smoke tests including webhooks-smoke.test.ts and sse-smoke.test.ts"
    - "Remaining E2E tests run without failure (or failures are diagnosed and fixed)"
  artifacts:
    - path: "build output"
      provides: "Clean TypeScript compilation after all OC reference removals"
    - path: "test output"
      provides: "Green test suite confirming smoke tests are valid"
  key_links:
    - from: "src/lib/__tests__/webhooks-smoke.test.ts"
      to: "src/lib/webhooks.ts"
      via: "import webhook functions"
      pattern: "import.*webhooks"
    - from: "src/lib/__tests__/sse-smoke.test.ts"
      to: "src/lib/event-bus.ts"
      via: "vi.mock of event bus"
      pattern: "vi.mock.*event-bus"
---

<objective>
Run build and test verification to close the FOUN-08 gap -- tests were written in Plan 03 but never executed.

Purpose: The verification report found that Plans 02 and 03 could not run pnpm build or pnpm test due to bash permission restrictions. This means all code changes from Plans 01-04 have never been build-verified, and the smoke tests created in Plan 03 have never been executed to confirm they pass. This plan runs the full verification suite and fixes any issues found.

Output: Green build, green unit tests, green E2E tests (or documented exceptions). Phase 1 verification gate fully passed.
</objective>

<execution_context>
@/Users/designmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/designmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-strip/01-VERIFICATION.md
@.planning/phases/01-foundation-strip/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and fix any compilation errors</name>
  <files></files>
  <action>
    Run `pnpm build` in the project root. If it succeeds, record the output. If it fails:

    1. Read the error output carefully -- TypeScript errors from the OC reference removals in Plan 04 are the most likely cause (missing imports after rename, type mismatches from removed aliases).
    2. Fix each error at its source:
       - Missing `runOpenClaw` import -> update to `runNanobot`
       - Missing `config.openclawX` property -> update to `config.nanobotX`
       - Type errors from renamed interfaces (OpenClawAgentConfig -> NanobotAgentConfig) -> update consumers
       - Any other cascading errors from Plan 04 changes
    3. Re-run `pnpm build` after each fix until it succeeds with exit code 0.

    Do NOT ignore or suppress errors. Every TypeScript error must be resolved.
  </action>
  <verify>
    <automated>cd /Users/designmac/projects/nanobot_mission_control && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    `pnpm build` exits with code 0. Zero TypeScript errors. The full OC removal from Plans 01-04 compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run unit tests and fix any failures</name>
  <files></files>
  <action>
    Run `pnpm test` in the project root. This should execute all Vitest unit tests including:
    - src/lib/__tests__/webhooks-smoke.test.ts (created in Plan 03, never run)
    - src/lib/__tests__/sse-smoke.test.ts (created in Plan 03, never run)
    - src/lib/__tests__/cron-occurrences.test.ts (updated in Plan 03)
    - Any other unit tests in the project

    If tests fail:
    1. Read the failure output to identify the root cause
    2. For import errors: likely caused by Plan 04 renames (runOpenClaw -> runNanobot, config property renames) -- update the test or the source
    3. For assertion errors in smoke tests: the test expectations may not match actual module exports -- fix the test to match the real API
    4. For unrelated failures: document but do not fix (they are pre-existing and not Phase 1 scope)
    5. Re-run `pnpm test` until all Phase 1-relevant tests pass

    If pnpm test does not exist as a script, check package.json for the correct test command (may be `pnpm vitest` or `pnpm test:unit`).
  </action>
  <verify>
    <automated>cd /Users/designmac/projects/nanobot_mission_control && pnpm test 2>&1 | tail -10</automated>
  </verify>
  <done>
    All unit/smoke tests pass. The webhook and SSE smoke tests from Plan 03 are confirmed working. The Phase 1 test verification gap is closed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual branding verification and Phase 1 sign-off</name>
  <action>
    Human verifies that Phase 1 Foundation Strip is visually complete:
    - Login page branding updated from OpenClaw to Nanobot Mission Control
    - Browser tab/meta description references Nanobot
    - E2E tests pass if infrastructure is available
  </action>
  <verify>
    1. Run `pnpm dev` and visit http://localhost:3000/login
       - Verify the login page says "Nanobot Mission Control" (NOT "OpenClaw Agent Orchestration")
    2. Check the browser tab title/meta -- should reference "Nanobot" not "OpenClaw"
    3. Optionally run `pnpm test:e2e` to verify E2E tests pass (if the E2E test infrastructure is set up with a running server)
    4. Confirm you are satisfied Phase 1 is complete and ready for Phase 2
  </verify>
  <done>
    User has approved the visual branding and confirmed Phase 1 Foundation Strip is complete. Type "approved" to close Phase 1, or describe any remaining issues.
  </done>
</task>

</tasks>

<verification>
Full Phase 1 verification gate:

```bash
# 1. Build
pnpm build

# 2. Unit tests
pnpm test

# 3. OC reference grep (should be 0 excluding test assertions and DB column)
grep -rn "openclaw\|OpenClaw\|OPENCLAW" src/ --include="*.ts" --include="*.tsx" | grep -v "__tests__" | grep -v "openclaw_home" | wc -l

# 4. E2E tests (if infrastructure available)
pnpm test:e2e
```
</verification>

<success_criteria>
- pnpm build exits 0 with zero TypeScript errors
- pnpm test passes all unit and smoke tests
- Login page visually confirmed to show Nanobot branding (human checkpoint)
- Phase 1 Foundation Strip declared complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-strip/01-05-SUMMARY.md`
</output>

---
phase: 01-foundation-strip
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/login/page.tsx
  - src/app/layout.tsx
  - src/components/layout/local-mode-banner.tsx
  - src/components/panels/settings-panel.tsx
  - src/lib/command.ts
  - src/lib/config.ts
  - src/lib/sessions.ts
  - src/lib/mentions.ts
  - src/lib/utils.ts
  - src/lib/validation.ts
  - src/lib/agent-templates.ts
  - src/lib/migrations.ts
  - src/lib/db.ts
  - src/app/api/status/route.ts
  - src/app/api/gateways/route.ts
  - src/app/api/gateways/health/route.ts
  - src/app/api/gateway-config/route.ts
  - src/app/api/notifications/deliver/route.ts
  - src/app/api/spawn/route.ts
  - src/app/api/tokens/route.ts
  - src/app/api/sessions/route.ts
  - src/app/api/logs/route.ts
  - src/app/api/tasks/[id]/broadcast/route.ts
  - src/app/api/agents/message/route.ts
  - src/app/api/agents/[id]/soul/route.ts
  - src/app/api/agents/[id]/wake/route.ts
  - src/app/api/agents/[id]/route.ts
  - src/app/api/pipelines/run/route.ts
  - src/components/panels/super-admin-panel.tsx
autonomous: true
requirements: [FOUN-01, FOUN-02]
gap_closure: true

must_haves:
  truths:
    - "Zero OpenClaw/openclaw/OPENCLAW string references remain in src/ production code (test assertion strings excluded)"
    - "User-visible branding says 'Nanobot Mission Control' not 'OpenClaw Agent Orchestration'"
    - "No API routes read OPENCLAW_* environment variables directly from process.env"
    - "runOpenClaw renamed to runNanobot in command.ts and all consumers updated"
  artifacts:
    - path: "src/lib/command.ts"
      provides: "runNanobot() function replacing runOpenClaw()"
      contains: "runNanobot"
    - path: "src/app/login/page.tsx"
      provides: "Login page with Nanobot branding"
      contains: "Nanobot Mission Control"
    - path: "src/app/layout.tsx"
      provides: "HTML metadata with Nanobot branding"
      contains: "Nanobot"
    - path: "src/lib/config.ts"
      provides: "Config with zero legacy aliases"
  key_links:
    - from: "src/app/api/status/route.ts"
      to: "src/lib/command.ts"
      via: "import runNanobot"
      pattern: "import.*runNanobot.*from.*command"
    - from: "src/app/api/gateways/route.ts"
      to: "src/lib/config.ts"
      via: "config.nanobotGatewayHost (or removed)"
      pattern: "config\\."
---

<objective>
Strip all remaining OpenClaw references from 31 src/ files to close the FOUN-01 verification gap.

Purpose: The verification report found 89 OC references across 31 files that were not targeted by the original Phase 1 plans. These include user-visible branding, active OC function names, OPENCLAW_* env var reads, and hardcoded OC infrastructure paths. This plan eliminates every remaining reference so the "zero OC code remains" truth is fully satisfied.

Output: Zero OC references in src/ production code (verified by grep). All function names, env var reads, branding, comments, and types updated to nanobot equivalents.
</objective>

<execution_context>
@/Users/designmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/designmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-strip/01-VERIFICATION.md
@.planning/phases/01-foundation-strip/01-01b-SUMMARY.md
@.planning/phases/01-foundation-strip/01-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs from prior plans. -->

From src/lib/config.ts (Plan 01-01b established NANOBOT_* env vars):
- config.nanobotHome (was openclawHome -- has legacy alias)
- config.nanobotStateDir (was openclawStateDir -- has legacy alias)
- config.nanobotBin (was openclawBin -- has legacy alias)
- config.nanobotConfigPath (was openclawConfigPath -- has legacy alias)
- config.clawdbotBin (separate, not OC)

From src/lib/command.ts:
```typescript
export function runOpenClaw(args: string[], options: CommandOptions = {})  // rename to runNanobot
export function runClawdbot(args: string[], options: CommandOptions = {})  // keep as-is
```

From src/types/shared.ts (Plan 01-02):
- Canonical entity types -- no OC references, already clean
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename core functions, remove config aliases, fix branding and lib files</name>
  <files>
    src/lib/command.ts
    src/lib/config.ts
    src/app/login/page.tsx
    src/app/layout.tsx
    src/components/layout/local-mode-banner.tsx
    src/components/panels/settings-panel.tsx
    src/lib/sessions.ts
    src/lib/mentions.ts
    src/lib/utils.ts
    src/lib/validation.ts
    src/lib/agent-templates.ts
    src/lib/migrations.ts
    src/lib/db.ts
  </files>
  <action>
    **command.ts:**
    - Rename `runOpenClaw` to `runNanobot` (line 74). Update its implementation to use `config.nanobotBin` (not `config.openclawBin`) and `config.nanobotStateDir` (not `config.openclawStateDir`).
    - Rename `runClawdbot` -- update line 84 to use `config.nanobotStateDir` instead of `config.openclawStateDir`.

    **config.ts:**
    - Remove the 4 legacy aliases entirely (lines 62, 64, 66, 70): `openclawHome`, `openclawStateDir`, `openclawBin`, `openclawConfigPath`. The nanobot equivalents already exist on the config object from Plan 01-01b. Any file still using the old aliases will be updated in this task or Task 2.

    **Branding:**
    - `src/app/login/page.tsx` line 180: Change "OpenClaw Agent Orchestration" to "Nanobot Mission Control"
    - `src/app/layout.tsx` line 14: Change description from "OpenClaw Agent Orchestration Dashboard" to "Nanobot Mission Control Dashboard"
    - `src/components/layout/local-mode-banner.tsx` line 16: Change "No OpenClaw gateway detected" to "No nanobot gateway detected"
    - `src/components/panels/settings-panel.tsx` line 20: Change "OpenClaw gateway connection settings" to "Gateway connection settings"

    **sessions.ts:**
    - Line 23: Change `config.openclawStateDir` to `config.nanobotStateDir`
    - Lines 49, 51-52: Update comments from "OpenClaw" to "nanobot" and `OPENCLAW_STATE_DIR` to `NANOBOT_STATE_DIR`

    **mentions.ts:**
    - Line 79: Rename `openclawId` variable to `agentId`
    - Lines 82-83: Update `parsed.openclawId` to `parsed.agentId` (or keep reading the field name from JSON but rename the local variable)
    - Line 89: Update variable reference

    **utils.ts:**
    - Line 62: Update comment from "OpenClaw 2026.3.x" to "Nanobot agent configs" (or similar neutral wording)

    **validation.ts:**
    - Line 54: Rename `openclaw_id` field to `agent_id` in the Zod schema, update the error message from "openclaw_id must be kebab-case" to "agent_id must be kebab-case"
    - Line 63: Rename `provision_openclaw_workspace` to `provision_workspace`
    - Line 64: Rename `openclaw_workspace_path` to `workspace_path`

    **agent-templates.ts:**
    - Line 5: Update comment from "OpenClaw agent config" to "nanobot agent config"
    - Line 46: Rename `OpenClawAgentConfig` interface to `NanobotAgentConfig`
    - Line 66: Update type reference
    - Lines 402, 418: Update comment and return type references

    **migrations.ts:**
    - Line 286: Keep the `openclaw_home` column name as-is in the SQL (renaming a DB column requires a migration, not a string replace). Add a comment: `-- legacy column name, aliased as nanobotHome in application layer`

    **db.ts:**
    - Line 208: Keep `openclaw_home` property in Tenant type (matches DB column). Add a JSDoc comment noting it is a legacy DB column name.
  </action>
  <verify>
    <automated>cd /Users/designmac/projects/nanobot_mission_control && grep -rn "openclawHome\|openclawStateDir\|openclawBin\|openclawConfigPath\|runOpenClaw\|OpenClaw Agent Orchestration\|OpenClawAgentConfig\|openclaw_id\|provision_openclaw" src/lib/ src/app/login/ src/app/layout.tsx src/components/ --include="*.ts" --include="*.tsx" | grep -v "node_modules" | grep -v "__tests__"</automated>
  </verify>
  <done>
    All core lib files, branding, and component files have zero OC references (except the 2 intentional DB column name references in db.ts and migrations.ts which are annotated).
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip OC references from all API routes</name>
  <files>
    src/app/api/status/route.ts
    src/app/api/gateways/route.ts
    src/app/api/gateways/health/route.ts
    src/app/api/gateway-config/route.ts
    src/app/api/notifications/deliver/route.ts
    src/app/api/spawn/route.ts
    src/app/api/tokens/route.ts
    src/app/api/sessions/route.ts
    src/app/api/logs/route.ts
    src/app/api/tasks/[id]/broadcast/route.ts
    src/app/api/agents/message/route.ts
    src/app/api/agents/[id]/soul/route.ts
    src/app/api/agents/[id]/wake/route.ts
    src/app/api/agents/[id]/route.ts
    src/app/api/pipelines/run/route.ts
    src/components/panels/super-admin-panel.tsx
  </files>
  <action>
    **status/route.ts (8 refs):**
    - Line 6: Change `runOpenClaw` import to `runNanobot`
    - Line 283: Change regex `/clawdbot|openclaw/i` to `/clawdbot|nanobot/i`
    - Line 345: Change regex `/clawdbot-gateway|openclaw-gateway|openclaw.*gateway/i` to `/clawdbot-gateway|nanobot-gateway|nanobot.*gateway/i`
    - Line 362: Change `runOpenClaw` call to `runNanobot`
    - Lines 508-510: Rename `openclawHome` variable to `nanobotHome`, use `config.nanobotStateDir` and `config.nanobotConfigPath`
    - Line 534: Update return object property from `openclawHome` to `nanobotHome`

    **gateways/route.ts (3 refs):**
    - Lines 56-59: Replace `process.env.OPENCLAW_GATEWAY_HOST` with `config.nanobotGatewayHost` or a neutral fallback. If no gateway config property exists in config.ts, use `process.env.NANOBOT_GATEWAY_HOST || '127.0.0.1'`. Same for OPENCLAW_GATEWAY_PORT -> `process.env.NANOBOT_GATEWAY_PORT || process.env.GATEWAY_PORT || '18789'`. Same for OPENCLAW_GATEWAY_TOKEN -> `process.env.NANOBOT_GATEWAY_TOKEN || process.env.GATEWAY_TOKEN || ''`.

    **gateways/health/route.ts (4 refs):**
    - Line 28: Change header check from `x-openclaw-version` to `x-nanobot-version` (keep `x-clawdbot-version` as fallback)
    - Line 35: Rename `hasOpenClaw32ToolsProfileRisk` to `hasToolsProfileRisk`
    - Line 105-106: Update function call and string from "OpenClaw 2026.3.2+" to "Nanobot agent" or similar

    **gateway-config/route.ts (3 refs):**
    - Line 10: Change `config.openclawConfigPath` to `config.nanobotConfigPath`
    - Lines 22, 62: Change error message from "OPENCLAW_CONFIG_PATH not configured" to "NANOBOT_CONFIG_PATH not configured"

    **notifications/deliver/route.ts (5 refs):**
    - Line 3: Change import from `runOpenClaw` to `runNanobot`
    - Lines 11, 82: Update comments from "OpenClaw" to "nanobot"
    - Line 84: Change `runOpenClaw` call to `runNanobot`
    - Line 97: Change error message from "OpenClaw error" to "Nanobot error"

    **spawn/route.ts (3 refs):**
    - Line 12: Change `OPENCLAW_TOOLS_PROFILE` to `NANOBOT_TOOLS_PROFILE`
    - Lines 38, 50: Update comments from "OpenClaw" to "nanobot"

    **tokens/route.ts (1 ref):**
    - Line 189: Update comment from "OpenClaw session stores" to "nanobot agent session stores"

    **sessions/route.ts (1 ref):**
    - Line 18: Update comment from "OpenClaw tracks" to "Agent tracks" or "Session tracker"

    **logs/route.ts (5 refs):**
    - Lines 22, 24: Update comments from "OpenClaw log formats" to "agent log formats", `.openclaw/workspace` to `.nanobot/workspace`
    - Line 26: Update comment "Gateway journal" keep format note but change "openclaw" to "nanobot"
    - Line 66: Update regex comment and pattern from `openclaw[PID]` to `nanobot[PID]`
    - Line 123: Update comment from "OpenClaw logs directory" to "nanobot logs directory"

    **tasks/[id]/broadcast/route.ts (2 refs):**
    - Line 3: Change import from `runOpenClaw` to `runNanobot`
    - Line 51: Change `runOpenClaw` call to `runNanobot`

    **agents/message/route.ts (2 refs):**
    - Line 3: Change import from `runOpenClaw` to `runNanobot`
    - Line 37: Change `runOpenClaw` call to `runNanobot`

    **agents/[id]/soul/route.ts (2 refs):**
    - Line 12: Change `config.openclawStateDir` to `config.nanobotStateDir`, change error message from "OPENCLAW_STATE_DIR" to "NANOBOT_STATE_DIR"
    - Line 13: Change `config.openclawStateDir` to `config.nanobotStateDir`

    **agents/[id]/wake/route.ts (2 refs):**
    - Line 3: Change import from `runOpenClaw` to `runNanobot`
    - Line 42: Change `runOpenClaw` call to `runNanobot`

    **agents/[id]/route.ts (1 ref):**
    - Line 50: Update comment from "OpenClaw agent config" to "nanobot agent config"

    **pipelines/run/route.ts (3 refs):**
    - Line 119: Update comment from "openclaw agent" to "nanobot agent"
    - Line 130: Change dynamic import to use `runNanobot` instead of `runOpenClaw`
    - Line 137: Change `runOpenClaw` call to `runNanobot`

    **super-admin-panel.tsx (6 refs):**
    - Lines 129, 366: Change default `owner_gateway` from `'openclaw-main'` to `'nanobot-main'`
    - Line 633: Update option value and display text from `openclaw-main` to `nanobot-main`
    - Line 1053: Change `.openclaw` to `.nanobot` in decommission description
    - Lines 1061-1062: Change `openclaw-gateway@` to `nanobot-gateway@` and `/etc/openclaw-tenants/` to `/etc/nanobot-tenants/`
  </action>
  <verify>
    <automated>cd /Users/designmac/projects/nanobot_mission_control && HITS=$(grep -rn "openclaw\|OpenClaw\|OPENCLAW" src/ --include="*.ts" --include="*.tsx" | grep -v "node_modules" | grep -v "__tests__" | grep -v "openclaw_home" | wc -l); echo "Remaining OC references (excluding DB column and test assertions): $HITS"; if [ "$HITS" -gt 0 ]; then grep -rn "openclaw\|OpenClaw\|OPENCLAW" src/ --include="*.ts" --include="*.tsx" | grep -v "node_modules" | grep -v "__tests__" | grep -v "openclaw_home"; fi</automated>
  </verify>
  <done>
    All API routes and super-admin panel have zero OC references. The only remaining "openclaw" strings in src/ are: (1) the `openclaw_home` DB column name in db.ts and migrations.ts (annotated as legacy), and (2) test assertion strings in smoke tests that verify no OC event types exist. A grep for openclaw/OpenClaw/OPENCLAW in src/ --include="*.ts" --include="*.tsx" excluding __tests__/ and openclaw_home returns 0 hits.
  </done>
</task>

</tasks>

<verification>
Run a comprehensive grep to verify zero OC references remain in production code:

```bash
# Should return 0 lines (excluding test assertions and DB column name)
grep -rn "openclaw\|OpenClaw\|OPENCLAW" src/ --include="*.ts" --include="*.tsx" | grep -v "__tests__" | grep -v "openclaw_home"
```

Verify branding is updated:
```bash
grep -n "OpenClaw" src/app/login/page.tsx src/app/layout.tsx src/components/layout/local-mode-banner.tsx
# Should return 0 lines
```

Verify runOpenClaw is gone:
```bash
grep -rn "runOpenClaw" src/ --include="*.ts" --include="*.tsx"
# Should return 0 lines
```
</verification>

<success_criteria>
- grep for openclaw/OpenClaw/OPENCLAW in src/ (excluding __tests__ and openclaw_home DB column) returns 0 hits
- Login page shows "Nanobot Mission Control" branding
- Layout metadata says "Nanobot Mission Control Dashboard"
- runOpenClaw renamed to runNanobot across entire codebase
- No OPENCLAW_* env var reads remain in any API route
- Config legacy aliases removed
- FOUN-01 and FOUN-02 verification gaps closed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-strip/01-04-SUMMARY.md`
</output>

---
phase: 06-overview-dashboard-and-remote-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/metric-strip.tsx
  - src/components/dashboard/activity-feed-inline.tsx
  - src/components/dashboard/error-summary-panel.tsx
  - src/components/layout/nav-rail.tsx
  - src/components/layout/header-bar.tsx
  - src/store/index.ts
autonomous: true
requirements: [DASH-02, DASH-03, DASH-04]

must_haves:
  truths:
    - "MetricStrip component renders agent count, status breakdown, total errors (24h), and active sessions from Zustand state"
    - "ActivityFeedInline component merges activities, lifecycle history, and status changes into a sorted, scrollable feed"
    - "ErrorSummaryPanel component groups errors by agent with collapse/expand and count badges"
    - "Nav rail no longer shows Agents, Webhooks, or Spawn items"
    - "Live-feed sidebar toggle removed from header and store"
  artifacts:
    - path: "src/components/dashboard/metric-strip.tsx"
      provides: "Summary metrics bar with agent counts, errors, sessions"
      min_lines: 40
    - path: "src/components/dashboard/activity-feed-inline.tsx"
      provides: "Inline SSE activity feed replacing sidebar"
      min_lines: 60
    - path: "src/components/dashboard/error-summary-panel.tsx"
      provides: "Error grouping by agent with collapse/expand"
      min_lines: 50
  key_links:
    - from: "src/components/dashboard/metric-strip.tsx"
      to: "src/store/index.ts"
      via: "useMissionControl() for discoveredAgents"
      pattern: "useMissionControl"
    - from: "src/components/dashboard/activity-feed-inline.tsx"
      to: "src/store/index.ts"
      via: "Zustand subscription for activities, lifecycleHistory"
      pattern: "useMissionControl"
    - from: "src/components/dashboard/error-summary-panel.tsx"
      to: "src/store/index.ts"
      via: "useMissionControl() for discoveredAgents errors"
      pattern: "discoveredAgents.*errors"
---

<objective>
Create the three new dashboard components (MetricStrip, ActivityFeedInline, ErrorSummaryPanel) and clean up nav rail, header bar, and Zustand store by removing legacy live-feed sidebar state and obsolete nav items.

Purpose: Build all the pieces needed by the new landing page (Plan 02) and remove dead code (live-feed sidebar, obsolete nav items) so the wiring plan has clean components to compose.
Output: Three new dashboard components ready for composition, cleaned nav rail, cleaned store.
</objective>

<execution_context>
@/Users/designmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/designmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-overview-dashboard-and-remote-access/06-CONTEXT.md
@.planning/phases/06-overview-dashboard-and-remote-access/06-RESEARCH.md

@src/store/index.ts
@src/components/layout/nav-rail.tsx
@src/components/layout/header-bar.tsx
@src/components/layout/live-feed.tsx
@src/components/agents/agent-summary-bar.tsx
@src/components/agents/agents-panel.tsx
@src/components/dashboard/dashboard.tsx
@src/lib/event-bus.ts
@src/lib/use-server-events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MetricStrip, ActivityFeedInline, and ErrorSummaryPanel components</name>
  <files>src/components/dashboard/metric-strip.tsx, src/components/dashboard/activity-feed-inline.tsx, src/components/dashboard/error-summary-panel.tsx</files>
  <action>
Create three new components in src/components/dashboard/:

**metric-strip.tsx:**
- Client component ("use client")
- Accepts `agents: AgentHealthSnapshot[]` as prop (or reads from useMissionControl)
- Computes: total agent count, healthy (green) count, degraded (yellow) count, errored (red) count, total errors in last 24h (sum of agent errors arrays), alive/active sessions count (process.level === 'green')
- Renders a horizontal strip of 4-5 metric cards above the agent grid
- Each metric card: icon, label, value, optional color coding (e.g., error count in red if > 0)
- Use Tailwind: flex row, gap-4, rounded cards with subtle border/shadow
- Pattern reference: existing MetricCard usage in dashboard.tsx and AgentSummaryBar in agent-summary-bar.tsx

**activity-feed-inline.tsx:**
- Client component ("use client")
- Subscribes to Zustand store for: activities, lifecycleHistory, discoveredAgents (for status change events)
- Merges events into a unified feed sorted by timestamp descending, capped at 50 items
- Event merge pattern from live-feed.tsx lines 13-42: map activities, lifecycle ops, and status changes into normalized feed items with {id, type, message, source, timestamp}
- All four event types: status changes, session activity, lifecycle actions, error events
- Renders as a scrollable list (max-h with overflow-y-auto) with each item showing: relative timestamp, agent icon/name, event description
- Use color coding per event type (green for start, red for errors/stops, blue for activity, yellow for warnings)
- Auto-scrolls to top on new events (prepend, not append)

**error-summary-panel.tsx:**
- Client component ("use client")
- Reads discoveredAgents from useMissionControl()
- Groups errors by agent: filter agents with non-empty errors array, map to {agentId, agentName, agentIcon, errors[], count}
- Sort by error count descending
- Render each agent as a collapsible group: header shows agent name + icon + count badge, click toggles showing individual errors
- Each error item shows: error message, timestamp (relative), severity if available
- Default: last 24h of errors (filter by timestamp)
- If no errors: show a "No errors in last 24h" message with a green checkmark
- Collapse/expand via local useState per agent group
  </action>
  <verify>
    <automated>cd /Users/designmac/projects/nanobot_mission_control && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Three new components exist, export default functions, type-check cleanly, and are ready to be imported by overview-landing.tsx in Plan 02</done>
</task>

<task type="auto">
  <name>Task 2: Clean up nav rail, header bar, and Zustand store (remove live-feed sidebar)</name>
  <files>src/components/layout/nav-rail.tsx, src/components/layout/header-bar.tsx, src/store/index.ts</files>
  <action>
**nav-rail.tsx:**
- Remove the 'agents' item from navGroups (landing page IS the agents page now)
- Remove the 'webhooks' item from navGroups
- Remove the 'spawn' item from navGroups
- Keep all other nav items intact (Tasks, Sessions, Tokens, Memory, Settings, etc.)
- Verify the 'overview' item remains and is the first/default item

**header-bar.tsx:**
- Find and remove the live-feed toggle button (the button that toggles the LiveFeed sidebar)
- Remove any imports of liveFeedOpen/toggleLiveFeed from the store
- If the toggle button is the only reference to live-feed, remove it cleanly without breaking layout

**store/index.ts:**
- Remove `liveFeedOpen: boolean` state property
- Remove `toggleLiveFeed` action
- Verify no other consumers reference these (grep first: `grep -rn 'liveFeedOpen\|toggleLiveFeed' src/` -- if only page.tsx and header-bar reference them, safe to remove)
- Keep all other store state intact (discoveredAgents, activities, lifecycleHistory, etc.)

Do NOT modify page.tsx or ContentRouter yet -- that happens in Plan 02.
Do NOT delete live-feed.tsx or dashboard.tsx yet -- deletion happens in Plan 02.
  </action>
  <verify>
    <automated>cd /Users/designmac/projects/nanobot_mission_control && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Nav rail shows no Agents/Webhooks/Spawn items. Header bar has no live-feed toggle. Store has no liveFeedOpen/toggleLiveFeed. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Three new component files exist in src/components/dashboard/
- Nav rail navGroups array has no 'agents', 'webhooks', or 'spawn' entries
- Store index.ts has no liveFeedOpen or toggleLiveFeed references
- grep for liveFeedOpen in src/ returns no hits (except possibly page.tsx which is updated in Plan 02)
</verification>

<success_criteria>
- MetricStrip computes and displays fleet-level metrics from agent data
- ActivityFeedInline merges SSE events into a scrollable inline feed
- ErrorSummaryPanel groups errors by agent with collapse/expand
- Nav rail cleaned of obsolete items
- Live-feed sidebar state fully removed from store and header
- All changes type-check cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-overview-dashboard-and-remote-access/06-01-SUMMARY.md`
</output>
